# ── Copy this file to .env and fill in your values ───────────────────────────
# NEVER commit .env to version control.

# ── Backend runtime ───────────────────────────────────────────────────────────
NODE_ENV=development
PORT=8080

# MongoDB connection URI
# For local docker-compose: mongodb://mongo:27017/dd_db
# For MongoDB Atlas:        mongodb+srv://<user>:<password>@cluster.mongodb.net/dd_db
MONGO_URI=mongodb://mongo:27017/dd_db

# Comma-separated list of allowed CORS origins
CORS_ORIGIN=http://localhost,http://localhost:4200

# ── CI/CD — set these as GitHub Actions Secrets, NOT here ─────────────────────
#
# DOCKERHUB_USERNAME   Your Docker Hub username
# DOCKERHUB_TOKEN      Docker Hub access token
#                      (Hub → Account Settings → Security → New Access Token)
#
# EC2_HOST             EC2 public IP or DNS name
# EC2_USER             SSH username  (ubuntu / ec2-user)
# EC2_SSH_KEY          Full PEM private key content
#                      (-----BEGIN RSA PRIVATE KEY----- … -----END RSA PRIVATE KEY-----)
#
# MONGO_URI            Same as above but for the production database
# CORS_ORIGIN          https://your-domain.com
